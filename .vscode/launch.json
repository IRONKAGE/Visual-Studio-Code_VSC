{
    // Best Solution for NodeJS (Найкраще рішення для Node.JS)
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "node.JS",
            "program": "${file}"
        }
    ]
}